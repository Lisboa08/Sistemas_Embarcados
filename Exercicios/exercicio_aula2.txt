Data: 09/03/2017

1. Por que o Linux recebeu esse nome?
Linux recebeu esse nome devido a uma junção do nome de seu criador, Linus, e Unix que tem ligação com um sistema operacional.

2. O que são daemons?
É um programa executado em plano de fundo, não estando no controle direto do usuário.

3. O que é o shell?
Shell é um progama que permite o usuário interagir por meio de comandos.

4. Por que é importante evitar executar o terminal como super-usuário?
Porque o super-usuário pode acessar todos  os niveis de segurança e diretórios, como por exemplo o root(/). Isso pode afetar o  funcionamento do sistema.

5. Qual botão do teclado completa o que o usuário escreve no terminal, de acordo com o contexto?
A tecla Tab.

6. Quais botões do teclado apresentam instruções escritas anteriormente?
A tecla de Seta para Cima e Seta para Baixo.

7. Apresente os respectivos comandos no terminal para:
  (a) Obter mais informações sobre um comando.
$ man

  (b) Apresentar uma lista com os arquivos dentro de uma pasta.
$ ls -l

  (c) Apresentar o caminho completo da pasta.
$ pwd

  (d) Trocar de pasta.
$ cd

  (e) Criar uma pasta.
$ file

  (f) Apagar arquivos definitivamente.
$ d

  (g) Apagar pastas definitivamente.

  (h) Copiar arquivos.
$ cp

  (i) Copiar pastas.
$cp -r

  (j) Mover arquivos.
$ mv

  (k) Mover pastas.
$ mv -r

  (l) Renomear pastas.
$ mv nome_antigo nome_novo

  (m) Apresentar o conteúdo de um arquivo.
$ cat, more ou less

  (n) Apresentar o tipo de um arquivo.
$ tail

  (o) Limpar a tela do terminal.
$ clear

  (p) Encontrar ocorrências de palavras-chave em um arquivo-texto.
$ grep

  (q) Ordenar informações em um arquivo-texto.
$ sort

  (r) Substituir ocorrências de palavras-chave em um arquivo-texto.
$ -ie sed 's / palavra1 / palavra2 / g' documento.txt

  (s) Conferir se dois arquivos são iguais.
$ diff

  (t) Escrever algo na tela.

